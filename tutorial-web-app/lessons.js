const lessons = [
 {title:'Introduction to computing',tag:'Build the digital foundations used by embedded systems.',summary:'Learn the number systems, digital logic, and computer building blocks that make microcontroller programming possible.',concepts:['Binary, decimal, and hexadecimal representation','ASCII and coded information','Logic levels, gates, adders, CPU, RAM, and ROM'],steps:['A bit has two states: 0 and 1. Group four bits to form a hexadecimal digit.','Use positional weights to translate a binary number to decimal.','Think of the CPU as the processing unit and RAM/ROM as working and permanent memory.'],example:'// 45 decimal is 0x2D (0010 1101 in binary)\nunsigned char value = 45;\nunsigned char mask = 0x0F;\nunsigned char lowNibble = value & mask;',quiz:'What is the hexadecimal form of binary 1111 0101?',answer:'F5'},
 {title:'The 8051 microcontroller',tag:'Meet the core architecture and its registers.',summary:'Explore the 8051 CPU, pin functions, memory spaces, and the registers that hold data and addresses.',concepts:['40-pin package and four I/O ports','Accumulator A, B register, R0-R7, DPTR and PC','Program memory, data RAM, SFRs, and register banks'],steps:['The accumulator is central to arithmetic and logic instructions.','R0-R7 belong to the selected register bank.','DPTR is a 16-bit pointer used when accessing code or external data.'],example:'#include <reg51.h>\n\nvoid main(void) {\n  P1 = 0x55;    // write to Port 1\n  while (1) {}\n}',quiz:'Which 16-bit register is commonly used as a data pointer?',answer:'DPTR'},
 {title:'8051 assembly language programming',tag:'Write, assemble, and run your first programs.',summary:'Understand the edit-assemble-test cycle, instruction layout, data movement, and assembler directives.',concepts:['Labels, mnemonics, operands, comments','MOV, ADD, INC, and data transfer','ORG, END, and program layout'],steps:['Place ORG at the start address for an assembly program.','In C, use named variables instead of CPU register names where practical.','Compile the source and program the generated HEX file.'],example:'#include <reg51.h>\n\nvoid main(void) {\n  unsigned char value = 0x25;\n  P1 = value;\n  while (1) {}\n}',quiz:'Which directive marks the end of an assembly source file?',answer:'END'},
 {title:'Branch instructions',tag:'Control program flow with decisions, loops, and calls.',summary:'Use conditional and unconditional jumps, loops, and subroutines to make 8051 programs responsive.',concepts:['SJMP, LJMP, AJMP and relative offsets','JZ, JNZ, DJNZ, CJNE, and bit tests','ACALL, LCALL, RET, and the stack'],steps:['Use DJNZ to decrement a counter and repeat while it is not zero.','In C, express decisions with if and repetition with loops.','Put reusable behavior in a function.'],example:'unsigned char i;\n\nfor (i = 0; i < 10; i++) {\n  P1 = i;\n}',quiz:'Which instruction both decrements a register and branches when it is not zero?',answer:'DJNZ'},
 {title:'I/O port programming',tag:'Read switches and drive LEDs or external devices.',summary:'Learn the behavior of ports P0-P3 and use byte and bit instructions for digital input and output.',concepts:['Quasi-bidirectional ports and Port 0 pull-ups','Read-modify-write behavior','SETB, CLR, CPL, JB, JNB, and JBC'],steps:['Write a 1 to a port pin before using it as an input.','Read the port and branch according to the input state.','Use bit instructions when one pin matters; use byte instructions for a whole port.'],example:'#include <reg51.h>\nsbit SW = P1^7;\nsbit LED = P2^0;\n\nvoid main(void) {\n  SW = 1;\n  while (1) if (SW == 0) LED = !LED;\n}',quiz:'Why should a port pin normally be written high before reading it as input?',answer:'It releases the pin so an external device can drive it.'},
 {title:'8051 addressing modes',tag:'Choose how each instruction finds its data.',summary:'Address registers, immediate constants, internal RAM, bits, and code-memory tables correctly.',concepts:['Immediate, register, direct, and register-indirect','Indexed addressing with MOVC','Bit addressing and SFR access'],steps:['Use # for an immediate constant in assembly.','In C, pointers make indirect memory access explicit.','Use a const array for a code-memory lookup table.'],example:'const unsigned char code table[] = {\'0\',\'1\',\'2\',\'3\'};\nunsigned char index = P1 & 0x03;\nP2 = table[index];',quiz:'Which registers may be used for register-indirect addressing of internal RAM?',answer:'R0 and R1'},
 {title:'Arithmetic and logic instructions',tag:'Manipulate values, bits, and BCD data.',summary:'Perform addition, subtraction, multiplication, division, logical operations, rotations, and decimal adjustments.',concepts:['Carry, auxiliary carry, overflow, and parity flags','ADD, ADDC, SUBB, MUL AB, DIV AB','ANL, ORL, XRL, CLR, CPL, RL/RR, and DA A'],steps:['Use unsigned types when working with raw 8-bit values.','Check range and overflow when the numeric result matters.','Use bitwise operators to mask or combine hardware bits.'],example:'unsigned char a = 0x35;\nunsigned char result = a - 0x12;  // 0x23\nP1 = result;\nP2 = (P2 & 0xF0) | 0x05;',quiz:'Which register pair is used by the MUL AB instruction?',answer:'Accumulator A and register B'},
 {title:'8051 programming in C',tag:'Use C while retaining hardware control.',summary:'Map C variables and operators to 8051 hardware registers and build portable embedded programs.',concepts:['Special function registers in reg51.h','while loops, delays, and bitwise operators','sfr and sbit declarations'],steps:['Include the device header that declares port registers.','Use bitwise operators for port manipulation.','Keep timing-sensitive hardware actions explicit and easy to review.'],example:'#include <reg51.h>\nvoid main(void) {\n  P1 = 0x55;\n  while(1) P1 = ~P1;\n}',quiz:'Which operator in C inverts every bit of a byte?',answer:'~'},
 {title:'Hardware connection and hex file',tag:'Connect, clock, reset, and program an 8051.',summary:'Understand the minimum 8051 system, oscillator and reset circuits, pins, and the generated Intel HEX file.',concepts:['Power, crystal oscillator, reset, and EA pin','Port pin alternate functions','Compiler output and programming memory'],steps:['Provide stable VCC, ground, oscillator, and reset connections.','Tie EA according to whether program code is internal or external.','Build the C source file and program the generated HEX file into supported memory.'],example:'#include <reg51.h>\n// Compile this main.c file to create a HEX file.\nvoid main(void) {\n  P1 = 0xAA;\n  while (1) {}\n}',quiz:'What does the EA pin select in an 8051 system?',answer:'Whether program code is fetched from internal or external memory'},
 {title:'Timer/counter programming',tag:'Generate delays and count external events.',summary:'Configure Timer 0 and Timer 1 modes, calculate reload values, and use timer flags in C.',concepts:['TMOD, TCON, THx, TLx, and TFx','Modes 0, 1, 2, and 3','Timer delays versus external pulse counting'],steps:['Select timer mode in TMOD.','Load TH/TL with the calculated initial value.','Start the timer, wait for its flag, then clear and reload as required.'],example:'#include <reg51.h>\n\nvoid delay(void) {\n  TMOD = (TMOD & 0xF0) | 0x01;\n  TH0 = 0xFC; TL0 = 0x66;\n  TF0 = 0; TR0 = 1;\n  while (!TF0);\n  TR0 = 0;\n}',quiz:'Which flag indicates that Timer 0 has overflowed?',answer:'TF0'},
 {title:'Serial port programming',tag:'Transmit and receive asynchronous serial data.',summary:'Set UART modes, derive a baud rate with Timer 1, and move bytes through SBUF.',concepts:['SCON, SBUF, TI, RI, and REN','UART frames: start, data, and stop bits','Baud-rate generation with Timer 1 mode 2'],steps:['Configure Timer 1 for the desired baud rate.','Set SCON for the UART mode and enable reception when needed.','Wait for TI after transmitting or RI after receiving, then clear the flag.'],example:'#include <reg51.h>\n\nunsigned char uart_read(void) {\n  while (!RI);\n  RI = 0;\n  return SBUF;\n}\n\nvoid main(void) { P1 = uart_read(); }',quiz:'Which bit signals that a serial byte has been received?',answer:'RI'},
 {title:'Interrupt programming',tag:'Respond to events without constant polling.',summary:'Enable, prioritize, and service external, timer, and serial interrupts with short, safe ISRs.',concepts:['IE and IP registers','Interrupt vector addresses','Polling versus interrupt-driven design'],steps:['Declare the service routine using the compiler interrupt syntax.','Enable the needed interrupt and global EA bit.','Keep the ISR short and avoid blocking work inside it.'],example:'#include <reg51.h>\nvolatile unsigned int ticks;\n\nvoid timer0_isr(void) interrupt 1 {\n  TF0 = 0;\n  ticks++;\n}\n\nvoid main(void) { EA = 1; ET0 = 1; TR0 = 1; while (1) {} }',quiz:'Which instruction returns from an interrupt service routine?',answer:'RETI'},
 {title:'External memory interfacing',tag:'Extend code and data space beyond the chip.',summary:'Use P0, P2, ALE, PSEN, RD, and WR to connect external ROM and RAM.',concepts:['Address/data multiplexing on Port 0','Latch use with ALE','MOVX for external data memory and MOVC for code memory'],steps:['Latch the low address byte from P0 using ALE.','Use P2 for the high address byte on a 16-bit address.','In C, use an xdata pointer for an external RAM location.'],example:'unsigned char xdata *ram =\n  (unsigned char xdata *)0x4000;\n\nvoid main(void) {\n  *ram = 0x55;\n  P1 = *ram;\n}',quiz:'Which instruction family accesses external data memory?',answer:'MOVX'},
 {title:'Real-world interfacing I: LCD, ADC, and sensors',tag:'Turn digital code into useful physical interfaces.',summary:'Connect character displays, sample analog signals, and interpret sensor data in a microcontroller system.',concepts:['LCD commands, data, RS, R/W, and E','ADC resolution and digital conversion','Sensor conditioning and polling'],steps:['Initialize the LCD before sending display data.','Start an ADC conversion and wait until it completes.','Scale the returned digital number into a meaningful sensor value.'],example:'#include <reg51.h>\nsbit RS = P2^0;\nsbit E  = P2^2;\n\nvoid lcd_command(unsigned char cmd) {\n  RS = 0; P1 = cmd; E = 1; E = 0;\n}',quiz:'What LCD control pin distinguishes a command from character data?',answer:'RS'},
 {title:'LCD and keyboard interfacing',tag:'Display messages and scan a matrix keypad.',summary:'Create human-facing input and output using LCD timing and row/column keyboard scanning.',concepts:['LCD initialization and cursor commands','Busy-flag versus delay timing','Matrix keypad scanning and debouncing'],steps:['Send LCD initialization commands with RS low.','Set RS high to write characters.','Drive one keypad row at a time and read columns to identify a pressed key.'],example:'#include <reg51.h>\n\nunsigned char keypad_read(void) {\n  P1 = 0xFE;           // drive row 0 low\n  if ((P1 & 0x10) == 0) return \'1\';\n  return 0;            // no key yet\n}',quiz:'How many I/O lines are needed for a 4x4 matrix keypad?',answer:'8'},
 {title:'8051 interfacing with 8255',tag:'Add programmable parallel I/O.',summary:'Use the 8255 PPI to increase the number and flexibility of digital ports, including ADC connections.',concepts:['Ports A, B, C and control register','Mode 0, Mode 1, and Mode 2','Chip select, address select, RD, and WR'],steps:['Select the 8255 register with A1 and A0.','Write a control word to configure direction and mode.','Read or write the selected port with external-memory instructions.'],example:'unsigned char xdata *control =\n  (unsigned char xdata *)0x8003;\nunsigned char xdata *portA =\n  (unsigned char xdata *)0x8000;\n\nvoid main(void) {\n  *control = 0x80;  // Mode 0, all outputs\n  *portA = 0x55;\n}',quiz:'Which 8255 register holds the configuration control word?',answer:'The control register'}
];
function nav(current){return lessons.map((x,i)=>`<a class="${i===current?'current':''}" href="lesson.html?lesson=${i}">${String(i+1).padStart(2,'0')} ${x.title}</a>`).join('')}
function quiz(i){const q=lessons[i];return `<section class="quiz"><h2>Quick check</h2><p>${q.quiz}</p><button type="button" onclick="this.nextElementSibling.hidden=false">Show answer</button><span class="feedback" hidden>${q.answer}</span></section>`}
function renderLesson(){let i=Math.max(0,Math.min(lessons.length-1,Number(new URLSearchParams(location.search).get('lesson'))||0)),x=lessons[i],prev=i?`<a href="lesson.html?lesson=${i-1}">← ${lessons[i-1].title}</a>`:'<span></span>',next=i<lessons.length-1?`<a href="lesson.html?lesson=${i+1}">${lessons[i+1].title} →</a>`:'<a href="index.html">Course home →</a>';document.title=`${x.title} | 8051 Tutorial`;document.getElementById('lesson-app').innerHTML=`<div class="lesson-layout"><nav class="toc" aria-label="Lessons">${nav(i)}</nav><main class="lesson"><a class="back" href="index.html">← All lessons</a><article><div class="kicker">Lesson ${String(i+1).padStart(2,'0')} of ${lessons.length}</div><h1>${x.title}</h1><p class="summary">${x.summary}</p><h2>In this lesson</h2><ul>${x.concepts.map(v=>`<li>${v}</li>`).join('')}</ul><h2>How to approach it</h2><ol>${x.steps.map(v=>`<li>${v}</li>`).join('')}</ol><div class="callout"><strong>Key idea</strong><br>${x.tag}</div><h2>Worked example</h2><pre><code>${x.example}</code></pre>${quiz(i)}<nav class="pager" aria-label="Lesson navigation">${prev}${next}</nav></article></main></div>`}
